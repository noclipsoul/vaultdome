[package]
name = "vaultdome"
version = "0.1.0"
edition = "2021"

[dependencies]
druid = "0.8" # Ensure this matches your version
reqwest = { version = "0.11", features = ["json"] }
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"